@using Web365.App_Code
@using Web365Utility
@using Web365Domain
@model Web365Models.ListArticleModel
@{
    Layout = null;
    var articleGroup = ViewBag.ArticleGroup as ArticleGroupItem;
    List<Web365Domain.ArticleItem> ltsArticle = ViewBag.ltsArticle;
}

<section class="container section__introduction row__up container__get">
    <div data-aos="fade-up" class="feature__container">
        <h2>Tính năng vượt trội trên ứng dụng <span>G-Store</span></h2>
        <p class="title">
            Tải ứng dụng ngay để trải nghiệm thực tế những tính năng hấp dẫn và
            thông minh để kết nối chia sẻ hàng hóa, dịch vụ với hàng ngàn người
            trên thế giới
        </p>
        <div class="row box__feature">
            @if (ltsArticle != null && ltsArticle.Any())
            {
                var i = 0;
                foreach (var item in ltsArticle)
                {
        <div class="col-xs-12 col-sm-6 col-lg-3 fixed">
            <div class="images">
                <img src="~/Content/GStoreWeb/images/img/images/features_icon_s.png" alt="" />
                <img class="img2" style="width:50px; height:50px;"  src="@Web365Utility.GetPathPicture(item.Picture.FileName)" alt=""/>
                                 @*src="~/Content/GStoreWeb/images/img/images/1-2.png" alt=""*@ 
            </div>
            <div class="content">
                <p>@item.Title</p>
                <p>
                    @Html.Raw(HiconHtmlHelper.TruncateContentInHtml(item.Summary, 700, "..."))
                </p>
            </div>
        </div>
                    {
                        i++;
                    }
                }
            }
        </div>
        </div>
</section>